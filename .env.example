# Copy to .env and fill in. .env is gitignored — never commit the real webhook.
#
# Discord: Server Settings -> Integrations -> Webhooks -> New Webhook -> Copy URL
# Anyone holding this URL can post to that channel, so treat it like a password.
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/000000000000000000/xxxxxxxxxxxxxxxxxxxxxxxx
